services:

  postgres:
    image: postgres:17.4
    ports:
      - 5432:5432
    # container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: book-store
